{
  "name": "crack402",
  "private": true,
  "workspaces": ["control-plane", "dashboard", "docs"],
  "scripts": {
    "docs:generate": "cd control-plane && bun run docs:openapi && cd ../docs && bun run docs:generate"
  }
}
